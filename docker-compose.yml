version: '3.4'

services:
    php-src:
        build: . 
        container_name: debug
        # options needed for gdb debugging
        security_opt: 
          - seccomp:unconfined
          - apparmor:unconfined
        cap_add:
          - ALL
        ports:
          - "5000:5000"
        volumes:
          - .:/root/work
        command:
          bash -c "tail -f /dev/null"
